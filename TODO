- Régler le problème d'incohérence des deux méthodes (Full mediation plus rapide qu'address translation. Résultat du nb d'appels système: un peu plus en full médiation: 5%, mais quand même plus rapide) TODO: vérifier les fonctions de temps, et ----> Profiler avec FlameGraph

- intégrer gettimeofday à simterpose Question: Est-ce que je suis assurée que la seule méthode de temps à laquelle le programme fait appel est gettimeofday?

- écrire tests (traces cohérentes? un server et plusieurs clients, +lent/+rapide; CONTINUER bittorent) ---> tesh

- est-ce que le pipe est vraiment utile entre ptrace et SG dans le dessin?

- cf ELKVM (Marion poster)
- port 53, DNS: intercepter tout ce qui arrive (ou bien écrire dans le resolv.conf de la machine physique)
- Modifier les dépendances entre tâches. Cf tuto SD (ou passer à MSG? ou rester comme ça avec le pseudo patch?)
- faire tableau des technos selon l'objectif d'interception (LD_PRELOAD, ptrace...)

Long terme:

- rejeu de traces
- utrace ?
- thread support (pas utiliser SimDag)


//////////////////// Anciennes tâches ////////////////////

Voir si on peut thread exit le thread maitre.

Ajouter un compteur de référence pour les fils descriptors.

Concentrer tous les print des syscall en un seul endroit pour pouvoir commenter.

Faire le tri des arguments que je parse qui me serve vraiment
